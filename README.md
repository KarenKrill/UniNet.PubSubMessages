# UniNet.PubSubMessages
A service for sending network messages in publisher/subscriber mode. Unlike the classic PubSub pattern, the publisher knows about subscribers and receives subscription events. The implementation is based on Mirror library, but abstractions allow for integration with other network libraries
